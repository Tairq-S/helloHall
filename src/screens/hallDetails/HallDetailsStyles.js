import { StyleSheet, Dimensions, Platform } from 'react-native';

export default HallDetailsStyles = StyleSheet.create({
    container: {
        flex:1,
        paddingVertical: 30,
        backgroundColor:'#000'
    },
    sliderContentContainer: {
        paddingVertical: 10
    },
    slider: {
        marginTop: 15,
        overflow: 'visible'
    },

})
