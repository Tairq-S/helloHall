import React, {Component} from 'react';
import Router from "./routing";
// import SplashScreen from 'react-native-splash-screen'
export default class AdPostApp extends Component {

    componentDidMount() {
    }
    render() {
        return (
            <Router />
        );
    }
}
